{
    "sites": [
        {
            "slug": "faint-itchy-hair",
            "distDir": "build",
            "buildCommand": "set NODE_OPTIONS=--openssl-legacy-provider PYTHON=D:envpythonpython-3.8.0python.exe && yarn build"
        }
    ]
}
